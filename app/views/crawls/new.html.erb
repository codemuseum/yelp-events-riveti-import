<h1>New crawl</h1>

<% form_for(@crawl) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :urls %><br />
    <%= f.text_area :urls %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', crawls_path %>