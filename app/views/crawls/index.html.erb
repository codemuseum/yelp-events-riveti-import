<h1>Listing crawls</h1>

<table>
  <tr>
    <th>Urls</th>
  </tr>

<% @crawls.each do |crawl| %>
  <tr>
    <td><%=h crawl.urls %></td>
    <td><%= link_to 'Show', crawl %></td>
    <td><%= link_to 'Edit', edit_crawl_path(crawl) %></td>
    <td><%= link_to 'Destroy', crawl, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New crawl', new_crawl_path %>